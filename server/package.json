{
  "name": "household-hub-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Read-only calendar + meals + chores hub for a home display",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "backup": "node backup.mjs"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "express": "^4.19.2"
  }
}
